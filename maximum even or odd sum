def max_odd_even_sum(head: ListNode) -> int:
    max_sum = float('-inf')
    current = head
    while current and current.next:
        pair_sum = current.val + current.next.val
        max_sum = max(max_sum, pair_sum)
        current = current.next.next
    return max_sum
